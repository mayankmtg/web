apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "packaged-app.fullname" . }}-test-connection"
  labels:
    {{- include "packaged-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "packaged-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
